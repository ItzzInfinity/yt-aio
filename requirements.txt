# Everything YT AIO needs to run.
#     python -m pip install -r requirements.txt
#
# ffmpeg is a separate, non-Python dependency and is not listed here. It is required to
# merge video with audio and to convert audio, and the README says how to install it.

PyQt6>=6.4
yt-dlp>=2024.1.1
mutagen>=1.45
